# Secrets - never commit
.env
*.token
.notion_token
*secret*
credentials*.json

# Your configured/exported workflows (may contain your database ID)
workflow/*.local.json
*-export.json

# OS / editor noise
*:Zone.Identifier
.DS_Store
Thumbs.db
